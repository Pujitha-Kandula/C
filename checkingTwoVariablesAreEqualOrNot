#include <stdio.h>

int main()
{
    int a,b;
    printf("Enter any two values: ");
    scanf("%d %d",&a,&b);
    if(a!=b)
    printf("The values are not equal");
    else
    printf("The values are equal");
    return 0;
}
