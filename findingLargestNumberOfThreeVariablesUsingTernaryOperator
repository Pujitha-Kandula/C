#include <stdio.h>

int main()
{
    int a,b,c,d;
    printf("Enter any three values: ");
    scanf("%d %d %d",&a,&b,&c);
    d=((a>b)?a:b)>c?((a>b)?a:b):c;
    printf("The largest number is %d",d);
    return 0;
}
