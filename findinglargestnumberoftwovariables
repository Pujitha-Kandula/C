#include <stdio.h>

int main()
{
    int a,b;
    printf("Enter any two values: ");
    scanf("%d %d",&a,&b);
    if(a>b)
    printf("The largest value is %d",a);
    else
    printf("The largest value is %d",b);
    return 0;
}
